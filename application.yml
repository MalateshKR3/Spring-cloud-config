server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MalateshKR3/Spring-cloud-config
          search-paths:
           - '*'
          #username: root
          #passphrase: 12345
